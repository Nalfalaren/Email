<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign up</title>
</head>

<body>
    <div class="signupBox">
        <h1>Sign up page</h1>
        <form action="/signup" method="POST">
            <label for="fullName">Full Name</label>
            <input type="text" placeholder="Enter the full name" name="fullName">
            <div class="errorResult">
                <% if(Object.keys(errorMessage).length> 0){ %>
                    <%= errorMessage.fullName %>
                <% } %>
            </div>
            <label for="account">Email</label>
            <input type="text" placeholder="Enter the account" name="email">
            <div class="errorResult">
                <% if(Object.keys(errorMessage).length> 0){ %>
                    <%= errorMessage.email %>
                <% } %>
            </div>
            <label for="password">Password</label>
            <input type="password" placeholder="Enter the password" name="password">
            <div class="errorResult">
                <% if(Object.keys(errorMessage).length> 0){ %>
                    <%= errorMessage.password %>
                <% } %>
            </div>
            <label for="signup">Confirm Password</label>
            <input type="password" placeholder="Confirm the password" name="confirmPassword">
            <div class="errorResult">
                <% if(Object.keys(errorMessage).length> 0){ %>
                    <%= errorMessage.confirmPassword %>
                <% } else { %>
                    <span style="color: green;"><%= successMessage.message %></span>
                    <% } %>
            </div>
            <div class="signupButton">
                <button class="saveButton">Save</button>
                <a href="/" class="returnButton">Sign in</a>
            </div>
        </form>
    </div>
</body>
<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: Arial;
    }

    label{
        font-weight: bold;
    }

    .signupBox {
        margin: 10em auto 6em;
        border: 1px solid rgb(103, 101, 101);
        max-width: 50%;
        border-radius: 20px;
    }


    h1 {
        font-size: clamp(1.25rem, 0.417rem + 3.333vw, 2.5rem);
        padding: 2rem;
        text-align: center;
    }

    form {
        display: flex;
        flex-direction: column;
        flex-direction: flex-start;
        gap: 0.5rem;
        padding: 1rem 4rem;
    }

    input {
        padding: 0.5rem 3rem;
        width: 90%;
        border-radius: 20px;
        margin-left: 20px;
    }

    input::placeholder {
        margin-right: 5rem;
    }

    .signupButton {
        display: flex;
        flex-direction: row;
        justify-content: center;
        align-items: center;
        gap: 3rem;
        padding-bottom: 1rem;
    }

    .saveButton,
    .returnButton {
        margin-top: 20px;
        padding: 1rem 2rem;
        background-color: rgb(113, 206, 31);
        border: 1px solid aquamarine;
        font-size: 1rem;
        color: black;
        font-weight: bold;
    }

    a {
        text-decoration: none;
        font-size: 1rem;
        color: black;
        font-weight: bold;
    }

    .errorResult{
        color: red;
        font-weight: bold;
    }
</style>

</html>